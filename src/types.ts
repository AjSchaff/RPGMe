export interface statBlock {
  stat: string;
  value: number;
  icon?: string;
}

export interface workHistoryEntry {
  title: string;
  timePeriod: string;
  image: string;
  accomplishments: string[];
}
